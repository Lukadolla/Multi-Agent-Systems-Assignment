agent VacOntology{
    module Console C;
    module System S;
	module EIS ei;
    module Debug debug;
    module Math M;

    types vac{
        formula squareInfo(string, string);
        formula locationInfo(int, int);
        formula startInfo(string);
    }

}